const boardPilesStyles = {
    boardPile: {
        width: (window.innerWidth/8) * 3,
        height: (window.innerHeight/6),
    },
    boardPiles: {
        width: (window.innerWidth/8) * 3,
        height: (window.innerHeight/6) * 4,
    }
}

export default boardPilesStyles