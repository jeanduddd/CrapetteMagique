const handStyles = {
    hand: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
        gap: window.innerWidth/20,
        height: window.innerHeight/6,
        width:"100vw",
    },
} as const

export default handStyles

